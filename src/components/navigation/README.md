# Shared Navigation Components (`src/components/navigation/`)

Holds reusable header, navbar, sidebar, profile drawer, and tab-bar controls that route or switch perspectives without duplicating layout elements.
